apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "git-bridge.fullname" . }}-test-connection"
  labels:
    {{- include "git-bridge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-q', '-O-', '{{ include "git-bridge.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
